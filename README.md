# wedding_project (public/)

Cloudflare Pages 用の静的サイトです。

- Build output directory: `public`
- noindex は `public/robots.txt` と `<meta name="robots">` で設定しています。
- 表示テキストは `public/config.js` を編集してください。
